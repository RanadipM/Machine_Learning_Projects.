# Rename to secrets.toml (Streamlit) or set as Space secrets (HuggingFace).
# The app runs WITHOUT these (grounded mock mode); add a key to enable live LLM.
GROQ_API_KEY = "your_groq_key_here"
GEMINI_API_KEY = "your_gemini_key_here"
